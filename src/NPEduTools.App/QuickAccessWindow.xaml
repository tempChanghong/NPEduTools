<Window x:Class="NPEduTools.App.QuickAccessWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="NPEduTools 快捷工具" Width="64" Height="84" WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True" Background="Transparent" ShowInTaskbar="False" ShowActivated="False" Topmost="True" FontFamily="Microsoft YaHei UI" Foreground="#202E35" UseLayoutRounding="True" AutomationProperties.AutomationId="QuickAccessWindow">
  <Grid Margin="6">
    <Button x:Name="EdgeHandle" Padding="0" MinHeight="0" Background="#21373F" Foreground="White" Click="HandleClicked" AutomationProperties.AutomationId="EdgeHandle" AutomationProperties.Name="展开快捷工具，可上下拖动">
      <Button.Template>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}" CornerRadius="16">
            <StackPanel VerticalAlignment="Center">
              <TextBlock Text="n" FontFamily="Segoe UI" FontSize="29" FontWeight="Bold" Foreground="#65DCBC" HorizontalAlignment="Center" Margin="0,-4,0,0" />
              <Border Width="14" Height="2" CornerRadius="1" Background="#627A81" Margin="0,6,0,0" />
            </StackPanel>
          </Border>
        </ControlTemplate>
      </Button.Template>
    </Button>
    <Border x:Name="PanelSurface" Visibility="Collapsed" Background="White" CornerRadius="18" Padding="20">
      <Border.Effect>
        <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.12" Color="#12272D" />
      </Border.Effect>
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Margin="0,0,0,18">
          <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <StackPanel VerticalAlignment="Center">
            <TextBlock Text="快捷工具" FontSize="18" FontWeight="SemiBold" />
            <TextBlock Text="NPEduTools" FontFamily="Segoe UI" FontSize="11" Foreground="#94A0A7" Margin="0,3,0,0" />
          </StackPanel>
          <Button x:Name="CollapseButton" Grid.Column="1" Content="›" FontSize="25" Width="44" Padding="0" Style="{StaticResource Quiet}" Click="CollapseClicked" AutomationProperties.AutomationId="CollapseQuick" AutomationProperties.Name="收起快捷工具" />
        </Grid>
        <Grid Grid.Row="1" Margin="0,0,0,14">
          <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition /></Grid.ColumnDefinitions>
          <Button x:Name="ToolsTab" Content="课堂工具" Padding="8" Click="ToolsTabClicked" AutomationProperties.AutomationId="QuickToolsTab" Margin="0,0,4,0" />
          <Button x:Name="ShortcutsTab" Grid.Column="1" Content="我的快捷" Padding="8" Click="ShortcutsTabClicked" AutomationProperties.AutomationId="QuickShortcutsTab" Margin="4,0,0,0" />
        </Grid>
        <ScrollViewer x:Name="ToolsPage" Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <StackPanel>
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
              </Grid.ColumnDefinitions>
              <Border Background="#FFF0E8" CornerRadius="10" Width="40" Height="40" Margin="0,0,12,0">
                <TextBlock Text="P" Foreground="#CC6944" FontFamily="Segoe UI" FontSize="23" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
              <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="触摸翻页" FontSize="16" FontWeight="SemiBold" />
                <TextBlock Text="Microsoft PowerPoint" Foreground="#87949C" FontFamily="Segoe UI" FontSize="11" Margin="0,3,0,0" />
              </StackPanel>
            </Grid>
            <TextBlock x:Name="StatusText" Text="正在连接后台…" FontSize="12" Foreground="#718089" TextWrapping="Wrap" Margin="0,16,0,16" AutomationProperties.AutomationId="QuickTouchStatus" />
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <Button x:Name="PowerButton" Content="开启辅助" Style="{StaticResource Primary}" Click="PowerClicked" AutomationProperties.AutomationId="QuickTouchPower" />
              <Button x:Name="PauseButton" Grid.Column="1" Content="暂停" Visibility="Collapsed" Margin="8,0,0,0" Click="PauseClicked" AutomationProperties.AutomationId="QuickTouchPause" />
            </Grid>
            <Border Height="1" Background="#EDF1F3" Margin="0,22,0,18" />
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <StackPanel VerticalAlignment="Center">
                <TextBlock Text="ClassIsland" FontFamily="Segoe UI" FontSize="16" FontWeight="SemiBold" />
                <TextBlock x:Name="ClassIslandStatus" Text="正在连接…" FontSize="11" Foreground="#87949C" Margin="0,4,6,0" TextWrapping="Wrap" />
              </StackPanel>
              <Button x:Name="ClassIslandButton" Grid.Column="1" Content="启动" Click="ClassIslandClicked" MinWidth="72" Padding="12,8" AutomationProperties.AutomationId="QuickStartClassIsland" />
            </Grid>
          </StackPanel>
        </ScrollViewer>
        <ScrollViewer x:Name="ShortcutsPage" Grid.Row="2" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <StackPanel>
            <StackPanel x:Name="ShortcutsEmpty" Margin="4,18">
              <TextBlock Text="把常用内容放到这里" FontSize="17" FontWeight="SemiBold" />
              <TextBlock Text="添加应用、课件或网址，下次点开就能用。" FontSize="13" Foreground="#718089" TextWrapping="Wrap" Margin="0,12,0,20" />
              <Button Content="添加快捷启动" Style="{StaticResource Primary}" Click="ManageShortcutsClicked" AutomationProperties.AutomationId="QuickAddShortcut" />
            </StackPanel>
            <ItemsControl x:Name="ShortcutItems">
              <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="2" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate><DataTemplate>
                <Button Content="{Binding}" ContentTemplate="{StaticResource ShortcutContent}" HorizontalContentAlignment="Stretch" Padding="8,12" Margin="0,0,5,6" MinHeight="76" ToolTip="{Binding Tip}" Click="ShortcutClicked" AutomationProperties.AutomationId="{Binding OpenId}" AutomationProperties.Name="{Binding Name}" />
              </DataTemplate></ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock x:Name="ShortcutStatus" FontSize="12" Foreground="#718089" TextWrapping="Wrap" Margin="0,10,0,0" AutomationProperties.AutomationId="QuickShortcutMessage" />
            <Button x:Name="ShortcutRepair" Content="编辑此项" Click="RepairShortcutClicked" Visibility="Collapsed" Margin="0,10,0,0" AutomationProperties.AutomationId="QuickRepairShortcut" />
          </StackPanel>
        </ScrollViewer>
        <Button x:Name="SettingsFooter" Grid.Row="3" Content="打开设置  →" Margin="0,16,0,0" HorizontalAlignment="Stretch" Style="{StaticResource Quiet}" Click="SettingsClicked" AutomationProperties.AutomationId="QuickOpenSettings" />
        <Button x:Name="ShortcutsFooter" Grid.Row="3" Content="管理快捷启动  →" Visibility="Collapsed" Margin="0,16,0,0" HorizontalAlignment="Stretch" Style="{StaticResource Quiet}" Click="ManageShortcutsClicked" AutomationProperties.AutomationId="QuickManageShortcuts" />
      </Grid>
    </Border>
  </Grid>
</Window>
